# Dotfiles TODO

## Up Next
- [ ] Research and install fuzzy search

## Backlog
- [ ] Build `gob` function — preprocess Obsidian markdown (WikiLinks, callouts, tags) in memory and pipe to `glow -` without editing the original file

## Done
- [x] Find a good font to use & add installation instructions
- [x] Setup vim configuration
- [x] Research and install interactive JSON tool
